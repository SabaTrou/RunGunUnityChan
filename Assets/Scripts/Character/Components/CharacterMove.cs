using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterMove : BaseMove
{
    protected override void MoveAction(Vector2 moveVec, float moveSpeed)
    {
        base.MoveAction(moveVec, moveSpeed);
    }
}
